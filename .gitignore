# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.next
dist
dist-ssr
*.local

# 실제 비밀은 `.env.local` 등 `*.local` 패턴에 걸리는 파일에만 두세요.
# `.env.example` 은 변수 이름·형식 안내용(플레이스홀더만)이므로 Git 에 포함합니다.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cursor / local tooling (not part of app)
.cursor/

# Python
__pycache__/

# TypeScript incremental
*.tsbuildinfo
tsconfig.tsbuildinfo
