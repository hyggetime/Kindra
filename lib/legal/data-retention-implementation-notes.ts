/**
 * 개인정보처리방침 §3·§6(보유·파기) 준수를 위한 구현 참고 메모.
 * 프로덕션 배포 전 법무·개인정보 담당 검토가 필요합니다.
 *
 * 정책 요약(방침 문구와 함께 유지):
 * - 리포트 제공 후: 이용자 삭제 요청 시 즉시 파기; 별도 요청 없으면 서비스 제공일로부터 1년 후 파기.
 * - 예외: 「아이 그림 보관함」「성장 갤러리」 등 지속 보관 동의, 또는 굿즈 제작 등 보존 요청이 있는 경우
 *   해당 서비스 제공 완료 시까지 보유.
 *
 * 구현 체크리스트(예시):
 * 1. DB·스토리지 메타데이터: 업로드·리포트 전달 시각, 파기 예정일(`retention_until`),
 *    연장 사유 코드(`vault` | `gallery` | `goods_request` 등), 동의 시각.
 * 2. 배치/Cron: 매일(또는 주기적으로) `retention_until` 경과 && 예외 플래그 없음 → Storage 객체 삭제 + DB 행 삭제.
 * 3. 1년 도래 N일 전: 이메일·앱 알림으로 삭제 vs 보관·굿즈 연장 선택 유도(제품 정책에 따라 선택).
 * 4. 삭제 시: Supabase Storage에서 원본 제거, 관련 DB 레코드 정리, 외부 AI·로그 보존 주기는 별도 위탁 계약과 정합.
 *
 * Vercel 예: `vercel.json`에 cron 경로 등록 후 Route Handler에서 위 배치만 수행(서버 전용 시크릿 사용).
 */

export {}
