/**
 * 홈(`/`) 상단 “○○의 마음 지도 다시 보기” 띠.
 *
 * 비활성화: localStorage 기반으로 `/report/{slug}` 로 이어지는 링크를 랜딩에 노출하지 않습니다.
 * (테스트·실제 리포트 URL 재유입 방지) 복원하려면 git 이력에서 `RecentReportBanner` 구현을 되살리면 됩니다.
 */
export function RecentReportBanner() {
  return null
}
